#include <stdio.h>
/**
 *main - Serves as the entry poin for the program
 *
 *Return: By default returns zero if no erros otherwise return non-zero value
 */
int main(void)
{
	puts("\"programming is like building a multilingual puzzle");
	return (0);
}

